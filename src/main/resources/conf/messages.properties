# Default messages resource
pippo.register = {hostname}{path}

pippo.passwordresetrequest = {hostname}{path}

pippo.collectioninvite = {frontend}/invitations.html

pippo.collectioninvitenewuser = {frontend}/login.html 

pippo.collectioninviteaction = {user} {action} {collection}.

pippo.notifyadminonverify = {email}
